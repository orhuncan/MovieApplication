/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';// aktarım yapılır
import {name as appName} from './app.json';// aktarım yapılır

AppRegistry.registerComponent(appName, () => App);
